services:
  helloagents:
    build:
      context: .
      dockerfile: Dockerfile
    image: helloagents:team1237.v1
    container_name: helloagents
    restart: unless-stopped
    volumes:
      - ./hello-agents:/learning/hello-agents
